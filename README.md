# C-2
Nested loops
# include < studio.h>
int main(){
     // limit for i
     int m = 4;
     //limit for j
     int n = 5;
     //outer loop
       for( int i=0; i<m; i++) {
       print f ("i = %d: ",i);
       // Inner Loop
         for( int j = 0; j < n; j++) {
         print f("%d ", i * n + j);
         } print f("/n");
         }
         return 0;
         }
